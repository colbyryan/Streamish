import React from "react";
import { Card } from "reactstrap"

const Favorites = ({ }) => {
    return (
        <Card >
            <p className="text-left px-2">Test</p>
        </Card>
    );
};

export default Favorites;